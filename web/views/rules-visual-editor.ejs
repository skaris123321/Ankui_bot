<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Визуальный редактор правил</title>
  <link rel="icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>🤖</text></svg>">
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="/css/embed-editor.css">
  <style>
    .rules-editor-page {
      padding: 2rem 0;
    }
    
    .rule-item {
      background-color: var(--background-dark);
      padding: 1.5rem;
      border-radius: 8px;
      margin-bottom: 1rem;
      border-left: 3px solid var(--primary-color);
    }
    
    .rule-item-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 1rem;
    }
    
    .rule-item-header h4 {
      margin: 0;
      color: var(--text-primary);
    }
    
    .rule-item-fields {
      display: grid;
      gap: 1rem;
    }
    
    .rule-item-fields input,
    .rule-item-fields textarea {
      width: 100%;
      padding: 0.75rem;
      background: var(--background);
      border: 1px solid rgba(255, 255, 255, 0.1);
      border-radius: 5px;
      color: var(--text-primary);
      font-family: inherit;
    }
    
    .rule-item-fields textarea {
      resize: vertical;
      min-height: 80px;
    }
    
    .remove-btn {
      background: #ED4245;
      color: white;
      border: none;
      padding: 0.5rem 1rem;
      border-radius: 5px;
      cursor: pointer;
      font-size: 0.9rem;
    }
    
    .remove-btn:hover {
      background: #c0392b;
    }
    
    #rulesContainer {
      margin-top: 1rem;
    }
    
    .image-block-section {
      background-color: var(--background-dark);
      padding: 1.5rem;
      border-radius: 8px;
      margin-bottom: 2rem;
      border-left: 3px solid #57F287;
    }
    
    .image-upload-group {
      display: flex;
      flex-direction: column;
      gap: 0.5rem;
    }
    
    .btn-upload {
      display: inline-block;
      padding: 0.5rem 1rem;
      background: rgba(88, 101, 242, 0.2);
      border: 1px solid rgba(88, 101, 242, 0.3);
      border-radius: 5px;
      color: var(--text-primary);
      cursor: pointer;
      text-align: center;
      transition: all 0.2s;
      width: fit-content;
    }
    
    .btn-upload:hover {
      background: rgba(88, 101, 242, 0.3);
      border-color: rgba(88, 101, 242, 0.5);
    }
    
    .file-name {
      color: var(--text-secondary);
      font-size: 0.9rem;
      margin-top: 0.25rem;
    }
  </style>
</head>
<body>
  <%- include('partials/header') %>
  
  <main class="rules-editor-page">
    <div class="container">
      <h1>📜 Визуальный редактор правил</h1>
      <p class="subtitle">Создавайте правила с картинкой и списком правил в одном сообщении</p>
      
      <div class="editor-layout">
        <!-- Левая панель - редактор -->
        <div class="editor-panel">
          <h2>✏️ Редактор</h2>
          
          <!-- Блок с картинкой -->
          <div class="image-block-section">
            <h3 style="margin-top: 0; margin-bottom: 1rem;">🖼️ Блок с картинкой (первый блок)</h3>
            
            <div class="editor-section">
              <label>📝 Заголовок (опционально)</label>
              <input type="text" id="imageBlockTitle" class="input-field" placeholder="Например: ВОЙС КАНАЛЫ" maxlength="256">
            </div>
            
            <div class="editor-section">
              <label>🖼️ Картинка *</label>
              <div class="image-upload-group">
                <input type="hidden" id="imageBlockImage" value="">
                <label for="imageBlockImageFile" class="btn btn-secondary btn-upload">Выбрать файл</label>
                <input type="file" id="imageBlockImageFile" accept="image/*" style="display: none;">
                <span id="imageBlockImageFileName" class="file-name"></span>
                <small style="color: var(--text-secondary); font-size: 12px;">Максимальный размер файла: 10 МБ. Поддерживаются форматы: JPEG, PNG, GIF, WEBP.</small>
              </div>
            </div>
          </div>
          
          <!-- Цвет боковой полоски -->
          <div class="editor-section">
            <label>🎨 Цвет боковой полоски</label>
            <div class="color-picker">
              <input type="color" id="rulesColor" value="#5865F2">
              <span id="rulesColorHex">#5865F2</span>
            </div>
            <div class="color-presets">
              <button class="color-btn" style="background: #5865F2" onclick="setRulesColor('#5865F2')">Discord</button>
              <button class="color-btn" style="background: #57F287" onclick="setRulesColor('#57F287')">Зелёный</button>
              <button class="color-btn" style="background: #ED4245" onclick="setRulesColor('#ED4245')">Красный</button>
              <button class="color-btn" style="background: #FEE75C" onclick="setRulesColor('#FEE75C')">Жёлтый</button>
              <button class="color-btn" style="background: #EB459E" onclick="setRulesColor('#EB459E')">Розовый</button>
              <button class="color-btn" style="background: #9B59B6" onclick="setRulesColor('#9B59B6')">Фиолетовый</button>
            </div>
          </div>
          
          <!-- Правила -->
          <div class="editor-section">
            <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 1rem;">
              <h3 style="margin: 0;">📋 Правила</h3>
              <button class="btn btn-primary" onclick="addRule()">➕ Добавить правило</button>
            </div>
            <div id="rulesContainer">
              <p style="color: var(--text-secondary); font-style: italic;">Нажмите "➕ Добавить правило" чтобы начать</p>
            </div>
          </div>
        </div>
        
        <!-- Правая панель - предпросмотр -->
        <div class="preview-panel">
          <h2>👁️ Предпросмотр</h2>
          <div class="discord-preview">
            <div class="discord-message">
              <div class="message-avatar">
                <img src="https://cdn.discordapp.com/embed/avatars/0.png" alt="Bot">
              </div>
              <div class="message-content">
                <div class="message-header">
                  <span class="message-username">Ваш Бот</span>
                  <span class="message-badge">БОТ</span>
                  <span class="message-timestamp">Сегодня в <%= new Date().toLocaleTimeString('ru-RU', { hour: '2-digit', minute: '2-digit' }) %></span>
                </div>
                <div id="embedPreview" class="embed-preview">
                  <!-- Embeds будут отображаться здесь -->
                </div>
              </div>
            </div>
          </div>
          
          <div class="action-buttons">
            <h3>📤 Отправка в Discord</h3>
            <div class="send-options">
              <label>Канал (ID)</label>
              <input type="text" id="targetChannel" class="input-field" placeholder="123456789012345678">
              <small>Скопируйте ID канала из Discord (ПКМ → Копировать ID)</small>
            </div>
            <button class="btn btn-primary btn-large" onclick="sendRules()">🚀 Отправить в Discord</button>
            <button class="btn btn-secondary btn-large" onclick="copyJSON()">📋 Копировать JSON</button>
          </div>
        </div>
      </div>
    </div>
  </main>
  
  <%- include('partials/footer') %>
  
  <script>
    let rules = [];
    let ruleCounter = 0;
    
    // Добавление правила
    function addRule() {
      const ruleId = ruleCounter++;
      rules.push({
        id: ruleId,
        number: '',
        description: '',
        punishment: '',
        duration: ''
      });
      renderRules();
      updatePreview();
    }
    
    // Удаление правила
    function removeRule(ruleId) {
      rules = rules.filter(r => r.id !== ruleId);
      renderRules();
      updatePreview();
    }
    
    // Отрисовка правил
    function renderRules() {
      const container = document.getElementById('rulesContainer');
      
      if (rules.length === 0) {
        container.innerHTML = '<p style="color: var(--text-secondary); font-style: italic;">Нажмите "➕ Добавить правило" чтобы начать</p>';
        return;
      }
      
      container.innerHTML = rules.map(rule => `
        <div class="rule-item" data-rule-id="${rule.id}">
          <div class="rule-item-header">
            <h4>Правило ${rule.id + 1}</h4>
            <button class="remove-btn" onclick="removeRule(${rule.id})">🗑️ Удалить</button>
          </div>
          <div class="rule-item-fields">
            <div>
              <label>Номер правила:</label>
              <input type="text" 
                     placeholder="Например: 2.1" 
                     value="${escapeHtml(rule.number)}"
                     oninput="updateRule(${rule.id}, 'number', this.value)">
            </div>
            <div>
              <label>Описание:</label>
              <textarea placeholder="Опишите правило..."
                        oninput="updateRule(${rule.id}, 'description', this.value)">${escapeHtml(rule.description)}</textarea>
            </div>
            <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 1rem;">
              <div>
                <label>Наказание:</label>
                <input type="text" 
                       placeholder="Например: Варн / Мут"
                       value="${escapeHtml(rule.punishment)}"
                       oninput="updateRule(${rule.id}, 'punishment', this.value)">
              </div>
              <div>
                <label>Длительность:</label>
                <input type="text" 
                       placeholder="Например: До 30 минут"
                       value="${escapeHtml(rule.duration)}"
                       oninput="updateRule(${rule.id}, 'duration', this.value)">
              </div>
            </div>
          </div>
        </div>
      `).join('');
    }
    
    // Обновление правила
    function updateRule(ruleId, field, value) {
      const rule = rules.find(r => r.id === ruleId);
      if (rule) {
        rule[field] = value;
        updatePreview();
      }
    }
    
    // Установка цвета
    function setRulesColor(color) {
      const colorInput = document.getElementById('rulesColor');
      const colorHex = document.getElementById('rulesColorHex');
      if (colorInput) colorInput.value = color;
      if (colorHex) colorHex.textContent = color;
      updatePreview();
    }
    
    // Обновление предпросмотра
    function updatePreview() {
      const preview = document.getElementById('embedPreview');
      if (!preview) return;
      
      const imageBlockTitle = document.getElementById('imageBlockTitle')?.value || '';
      const imageBlockImage = document.getElementById('imageBlockImage')?.value || '';
      const rulesColorEl = document.getElementById('rulesColor');
      const rulesColor = rulesColorEl ? rulesColorEl.value : '#5865F2';
      
      // Если нет картинки и нет правил, показываем сообщение
      if (!imageBlockImage && rules.length === 0) {
        preview.innerHTML = '<div class="empty">Начните добавлять блок с картинкой и правила...</div>';
        preview.classList.add('empty');
        return;
      }
      
      preview.classList.remove('empty');
      
      let html = '';
      
      // Первый блок - картинка
      if (imageBlockImage) {
        html += `<div class="embed" style="border-left: 4px solid ${rulesColor};">`;
        if (imageBlockTitle) {
          html += `<div class="embed-title">${escapeHtml(imageBlockTitle)}</div>`;
        }
        html += `<div class="embed-image-container">`;
        html += `<img src="${imageBlockImage}" class="embed-image" onerror="this.style.display='none'; this.parentElement.innerHTML='<div style=\\'color: #ED4245; padding: 1rem;\\'>❌ Не удалось загрузить изображение</div>';" style="max-width: 100%; border-radius: 4px;">`;
        html += `</div>`;
        html += '</div>';
      }
      
      // Блоки с правилами
      rules.forEach(rule => {
        if (!rule.description && !rule.number) return; // Пропускаем пустые правила
        
        html += `<div class="embed" style="border-left: 4px solid ${rulesColor}; margin-top: 1rem;">`;
        
        const ruleNumber = rule.number || '';
        const description = rule.description || '';
        
        let fieldValue = description ? `**${escapeHtml(description)}**` : '';
        
        if (rule.punishment || rule.duration) {
          fieldValue += '<br>';
          if (rule.punishment) {
            fieldValue += `<br>**Наказание:** ${escapeHtml(rule.punishment)}`;
          }
          if (rule.duration) {
            fieldValue += `<br>⏱️ **Длительность:** ${escapeHtml(rule.duration)}`;
          }
        }
        
        html += `<div class="embed-field">`;
        html += `<div class="embed-field-name">${ruleNumber ? `Правило - ${escapeHtml(ruleNumber)}` : 'Правило'}</div>`;
        html += `<div class="embed-field-value">${fieldValue}</div>`;
        html += `</div>`;
        
        html += '</div>';
      });
      
      preview.innerHTML = html;
    }
    
    // Отправка правил в Discord
    async function sendRules() {
      const channelId = document.getElementById('targetChannel')?.value.trim();
      
      if (!channelId) {
        alert('❌ Введите ID канала');
        return;
      }
      
      const imageBlockTitle = document.getElementById('imageBlockTitle')?.value || '';
      const imageBlockImage = document.getElementById('imageBlockImage')?.value || '';
      
      if (!imageBlockImage) {
        alert('❌ Добавьте картинку в первый блок');
        return;
      }
      
      if (rules.length === 0 || !rules.some(r => r.description)) {
        alert('❌ Добавьте хотя бы одно правило');
        return;
      }
      
      // Получаем выбранный цвет
      const rulesColorEl = document.getElementById('rulesColor');
      const rulesColor = rulesColorEl ? rulesColorEl.value : '#5865F2';
      const rulesColorInt = parseInt(rulesColor.replace('#', ''), 16);
      
      // Создаем embeds
      const embeds = [];
      
      // Первый embed - картинка
      // Добавляем пустое поле, чтобы структура совпадала с остальными embeds
      // Это заставит Discord выровнять все embeds по одной ширине
      const imageEmbed = {
        color: rulesColorInt,
        image: {
          url: imageBlockImage
        },
        fields: [{
          name: '\u200b', // Невидимый символ - пустое поле для выравнивания
          value: '\u200b',
          inline: false
        }]
      };
      
      if (imageBlockTitle) {
        imageEmbed.title = imageBlockTitle;
      }
      
      embeds.push(imageEmbed);
      
      // Embeds с правилами
      rules.forEach(rule => {
        if (!rule.description) return;
        
        const ruleNumber = rule.number || '';
        const description = rule.description || '';
        
        let fieldValue = `**${description}**`;
        
        if (rule.punishment || rule.duration) {
          fieldValue += '\n';
          if (rule.punishment) {
            fieldValue += `\n**Наказание:** ${rule.punishment}`;
          }
          if (rule.duration) {
            fieldValue += `\n⏱️ **Длительность:** ${rule.duration}`;
          }
        }
        
        const ruleEmbed = {
          color: rulesColorInt,
          fields: [{
            name: ruleNumber ? `Правило - ${ruleNumber}` : 'Правило',
            value: fieldValue,
            inline: false
          }]
        };
        
        embeds.push(ruleEmbed);
      });
      
      try {
        const response = await fetch('/api/send-rules', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json'
          },
          body: JSON.stringify({
            channelId: channelId,
            embeds: embeds
          })
        });
        
        const result = await response.json();
        
        if (result.success) {
          alert('✅ Правила успешно отправлены в Discord!');
        } else {
          alert('❌ Ошибка: ' + (result.message || 'Неизвестная ошибка'));
        }
      } catch (error) {
        console.error('Ошибка отправки правил:', error);
        alert('❌ Ошибка при отправке правил: ' + error.message);
      }
    }
    
    // Копирование JSON
    function copyJSON() {
      const imageBlockTitle = document.getElementById('imageBlockTitle')?.value || '';
      const imageBlockImage = document.getElementById('imageBlockImage')?.value || '';
      const rulesColorEl = document.getElementById('rulesColor');
      const rulesColor = rulesColorEl ? rulesColorEl.value : '#5865F2';
      const rulesColorInt = parseInt(rulesColor.replace('#', ''), 16);
      
      const embeds = [];
      
      if (imageBlockImage) {
        const imageEmbed = {
          color: rulesColorInt,
          image: { url: imageBlockImage },
          fields: [{
            name: '\u200b', // Невидимый символ - пустое поле для выравнивания
            value: '\u200b',
            inline: false
          }]
        };
        if (imageBlockTitle) {
          imageEmbed.title = imageBlockTitle;
        }
        embeds.push(imageEmbed);
      }
      
      rules.forEach(rule => {
        if (!rule.description) return;
        
        const ruleNumber = rule.number || '';
        const description = rule.description || '';
        let fieldValue = `**${description}**`;
        
        if (rule.punishment || rule.duration) {
          fieldValue += '\n';
          if (rule.punishment) fieldValue += `\n**Наказание:** ${rule.punishment}`;
          if (rule.duration) fieldValue += `\n⏱️ **Длительность:** ${rule.duration}`;
        }
        
        embeds.push({
          color: rulesColorInt,
          fields: [{
            name: ruleNumber ? `Правило - ${ruleNumber}` : 'Правило',
            value: fieldValue,
            inline: false
          }]
        });
      });
      
      const json = JSON.stringify({ embeds: embeds }, null, 2);
      navigator.clipboard.writeText(json).then(() => {
        alert('✅ JSON скопирован в буфер обмена!');
      }).catch(err => {
        console.error('Ошибка копирования:', err);
        alert('❌ Не удалось скопировать JSON');
      });
    }
    
    // Экранирование HTML
    function escapeHtml(text) {
      const div = document.createElement('div');
      div.textContent = text;
      return div.innerHTML;
    }
    
    // Функция загрузки файла
    async function uploadFile(file) {
      return new Promise((resolve, reject) => {
        // Проверяем размер файла (максимум 10 МБ)
        if (file.size > 10 * 1024 * 1024) {
          reject(new Error('Размер файла не должен превышать 10 МБ'));
          return;
        }
        
        // Проверяем тип файла
        const allowedTypes = ['image/jpeg', 'image/png', 'image/gif', 'image/webp', 'image/jpg'];
        if (!allowedTypes.includes(file.type)) {
          reject(new Error('Разрешены только изображения (JPEG, PNG, GIF, WEBP)'));
          return;
        }
        
        // Создаём FormData для отправки файла
        const formData = new FormData();
        formData.append('image', file);
        
        // Отправляем файл на сервер
        fetch('/api/upload-image', {
          method: 'POST',
          body: formData
        })
        .then(response => {
          if (!response.ok) {
            throw new Error(`HTTP error! status: ${response.status}`);
          }
          return response.json();
        })
        .then(result => {
          if (result.success) {
            // Возвращаем полный URL (с доменом для Discord)
            let url = result.url;
            if (!url.startsWith('/')) {
              url = '/' + url;
            }
            const fullUrl = window.location.origin + url;
            resolve(fullUrl);
          } else {
            reject(new Error(result.message || 'Ошибка загрузки'));
          }
        })
        .catch(error => {
          console.error('Ошибка загрузки файла:', error);
          reject(error);
        });
      });
    }
    
    // Обработчик загрузки изображения
    document.addEventListener('DOMContentLoaded', () => {
      const imageFileInput = document.getElementById('imageBlockImageFile');
      const titleInput = document.getElementById('imageBlockTitle');
      
      if (imageFileInput) {
        imageFileInput.addEventListener('change', async function(e) {
          const file = e.target.files[0];
          if (!file) return;
          
          const fileNameSpan = document.getElementById('imageBlockImageFileName');
          if (fileNameSpan) {
            fileNameSpan.textContent = `⏳ Загрузка ${file.name}...`;
          }
          
          try {
            const url = await uploadFile(file);
            const hiddenInput = document.getElementById('imageBlockImage');
            if (hiddenInput) {
              hiddenInput.value = url;
            }
            if (fileNameSpan) {
              fileNameSpan.textContent = `✅ ${file.name} загружено (${(file.size / 1024).toFixed(1)} КБ)`;
            }
            updatePreview();
          } catch (error) {
            console.error('Ошибка загрузки:', error);
            if (fileNameSpan) {
              fileNameSpan.textContent = `❌ Ошибка: ${error.message}`;
            }
            e.target.value = '';
          }
        });
      }
      
      if (titleInput) {
        titleInput.addEventListener('input', updatePreview);
      }
      
      // Обработчик изменения цвета
      const rulesColorInput = document.getElementById('rulesColor');
      const rulesColorHex = document.getElementById('rulesColorHex');
      
      if (rulesColorInput && rulesColorHex) {
        rulesColorInput.addEventListener('input', function(e) {
          rulesColorHex.textContent = e.target.value;
          updatePreview();
        });
        
        // Инициализация hex значения
        rulesColorHex.textContent = rulesColorInput.value;
      }
      
      updatePreview();
    });
  </script>
</body>
</html>

