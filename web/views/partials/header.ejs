<header class="header">
  <div class="container">
    <nav class="navbar">
      <div class="navbar-left">
        <div class="logo">
          <a href="/">🤖 Discord Bot</a>
        </div>
        <ul class="nav-menu">
          <li><a href="/dashboard" class="<%= page === 'dashboard' ? 'active' : '' %>">Панель управления</a></li>
        </ul>
      </div>
      <div class="user-info">
        <% if (user) { %>
          <span>👤 <%= user.username %></span>
        <% } else { %>
          <span>🤖 Бот</span>
        <% } %>
      </div>
    </nav>
  </div>
</header>

