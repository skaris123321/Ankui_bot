# Вход в проект
cd C:\Users\Катя\Desktop\Bot

# Или клонирование репозитория (если еще не клонирован)
# git clone https://github.com/ВАШ_ЮЗЕРНЕЙМ/ВАШ_РЕПОЗИТОРИЙ.git
# cd Bot

# Коммит и пуш изменений
git add -A
git commit -m "Fix: Database loading issue and improve data persistence logging - Remove unnecessary db.load() call in stats command that was overwriting in-memory data - Add userLevels count logging in save() and load() methods to track data persistence - Ensure userLevels field exists when loading database to prevent data loss - Improve database initialization to always include userLevels field - Add fallback to load from file only if data is missing in memory"
git push origin main
