# Вход в проект
cd C:\Users\Катя\Desktop\Bot

# Или клонирование репозитория (если еще не клонирован)
# git clone https://github.com/ВАШ_ЮЗЕРНЕЙМ/ВАШ_РЕПОЗИТОРИЙ.git
# cd Bot

# Коммит и пуш изменений
git add -A
git commit -m "Fix: Improve /stats command response handling and add error recovery - Add error handling for 'no data' message sending with fallback to followUp - Add logging to track if interaction was properly deferred/replied - Improve error recovery when editReply fails for empty stats - Add detailed logging for command execution status - Fix response delivery to ensure user always gets feedback"
git push origin main
