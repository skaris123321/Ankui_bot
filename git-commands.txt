# Вход в проект
cd C:\Users\Катя\Desktop\Bot

# Или клонирование репозитория (если еще не клонирован)
# git clone https://github.com/ВАШ_ЮЗЕРНЕЙМ/ВАШ_РЕПОЗИТОРИЙ.git
# cd Bot

# Коммит и пуш изменений
git add -A
git commit -m "Fix: Improve interaction duplicate handling and add comprehensive stats logging - Reorder checks to verify deferred/replied status BEFORE attempting deferReply - Add detailed logging for all users with their messages and voiceTime values - Log user filtering at each step (total users, after server filter, after sorting, after data filter) - Improve duplicate interaction detection by checking status first - Add per-user logging to debug why stats show 'no data' when users exist"
git push origin main
