# Вход в проект
cd C:\Users\Катя\Desktop\Bot

# Или клонирование репозитория (если еще не клонирован)
# git clone https://github.com/ВАШ_ЮЗЕРНЕЙМ/ВАШ_РЕПОЗИТОРИЙ.git
# cd Bot

# Коммит и пуш изменений
git add -A
git commit -m "Fix: Add comprehensive database diagnostics and improve data tracking - Add detailed logging in addUserMessage to track when users are saved - Add database file path logging in stats command for debugging - Add key listing and server-specific key counting for diagnostics - Ensure userLevels exists before adding messages to prevent data loss - Add logging of total users in database after each save operation"
git push origin main
