# Вход в проект
cd C:\Users\Катя\Desktop\Bot

# Или клонирование репозитория (если еще не клонирован)
# git clone https://github.com/ВАШ_ЮЗЕРНЕЙМ/ВАШ_РЕПОЗИТОРИЙ.git
# cd Bot

# Коммит и пуш изменений
git add -A
git commit -m "Fix: Interaction timeout (10062) by removing logging before deferReply - Remove console.log before deferReply() to prevent interaction timeout - Move logging after deferReply() to ensure response within 3 seconds - Fix statistics collection: now always tracks messages and voice time - Add extensive logging to debug slash commands and stats data"
git push origin main
