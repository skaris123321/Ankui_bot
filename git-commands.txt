# Вход в проект
cd C:\Users\Катя\Desktop\Bot

# Или клонирование репозитория (если еще не клонирован)
# git clone https://github.com/ВАШ_ЮЗЕРНЕЙМ/ВАШ_РЕПОЗИТОРИЙ.git
# cd Bot

# Коммит и пуш изменений
git add -A
git commit -m "Fix: Improve /stats command error handling and prevent timeouts - Add database availability checks before operations - Remove expensive JSON.stringify logging that could cause delays - Improve error handling for database loading and embed creation - Add null checks for guild and channel data - Improve user filtering with additional safety checks - Add better error messages for edge cases - Fix reply error handling to prevent unhandled exceptions"
git push origin main
