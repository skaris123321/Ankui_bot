# Вход в проект
cd C:\Users\Катя\Desktop\Bot

# Или клонирование репозитория (если еще не клонирован)
# git clone https://github.com/ВАШ_ЮЗЕРНЕЙМ/ВАШ_РЕПОЗИТОРИЙ.git
# cd Bot

# Коммит и пуш изменений
git add -A
git commit -m "Fix: Add detailed logging and improve stats filtering logic - Add comprehensive logging for user filtering at each step (before/after sorting) - Log individual user voiceTime values for debugging voice stats - Improve 'no data' messages to be type-specific (messages vs voice) - Fix stats display logic to properly filter users with data > 0 - Add debug logging to identify why stats show 'no data' when users exist"
git push origin main
